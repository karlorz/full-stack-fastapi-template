external-secrets:
  serviceAccount:
    name: "external-secrets"
    annotations:
      eks.amazonaws.com/role-arn: "arn:aws:iam::961341535962:role/external-secrets-role-684ad2e"
config:
  kind: ClusterSecretStore
  stores:
    parameter-store:
      provider:
        aws:
          service: ParameterStore
          region: us-east-1
